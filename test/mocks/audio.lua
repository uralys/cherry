local audio = {
  loadSound = function(path) return {} end,
  pause = function(path) return {} end,
  play = function(file, options) return {} end,
  setVolume = function(amount, channel) return true end
}

return audio
